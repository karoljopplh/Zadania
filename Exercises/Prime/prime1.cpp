/*
In this program, the user is prompted to enter the starting and ending values of a range.
The program then finds and prints all the prime numbers within that range.
The isPrime function is used to check if a given number is prime.
*/

#include <iostream>

using namespace std;

void isPrime(int a)
{
	if(a%)
}

int main()
{
	int x,y;
	
	cout<<"Enter the starting range value";
	cin>>x;
	cout<<"Enter the ending range value";
	cin>>y;
	
	return 0;
}